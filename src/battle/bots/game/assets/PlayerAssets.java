package battle.bots.game.assets;

import java.io.IOException;

/**
 * Contains player-related assets
 * @author Harry Xu
 * @version 1.0 - March 23rd 2024
 */
public class PlayerAssets {
    /**
     * Constructs a {@link PlayerAssets} instance with a sprite size
     * @param size the size to resize the sprites to
     * @throws IOException if an I/O error occur while reading the files
     */
    public PlayerAssets(int size) throws IOException {
        // TODO
        // Map<String, Image[]> sprites = Assets.loadAndGroupImages(Paths.get("<filepath>"), size);
    }
}
